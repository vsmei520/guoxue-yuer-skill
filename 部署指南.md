# 曾美团队·育儿国风穿越 - 部署指南

## 📋 系统概览

**架构说明**：
- 服务端：提供 OAuth 认证 + 返回 workflow 文件
- 客户端：通过 OAuth 认证，获取 workflow 后本地执行
- AI 调用：使用用户在 Claude Desktop 配置的 API 密钥

**优势**：
- ✅ 用户不需要单独配置 API
- ✅ 核心规则保护在服务器
- ✅ 标准 MCP OAuth 协议
- ✅ 数据持久化（SQLite）

---

## 🚀 服务端部署（宝塔版）

### 第一步：准备服务器环境

**要求**：
- Node.js >= 22.0.0
- 域名：guoxue.073955.com
- 已安装宝塔面板

**检查 Node.js 版本**：
```bash
node -v  # 应该显示 v22.x.x 或更高
```

如果版本低于 22，需要升级（Node.js 22 才有内置的 `node:sqlite`）。

---

### 第二步：上传并解压

1. **打开宝塔文件管理**
2. 进入：`/www/wwwroot/guoxue.073955.com`
3. **删除旧文件**（如果有）
4. **上传**：`guoxue-server-oauth.zip`
5. **解压**

**解压后的目录结构**：
```
/www/wwwroot/guoxue.073955.com/
├── src/
│   ├── server.js          # 主服务器
│   └── license-service.js # 授权管理
├── workflows/
│   ├── guoxue-chuanyue-main.md     # 核心规则
│   └── guoxue-chuanyue-examples.md # 范例库
├── package.json
├── ecosystem.config.js    # PM2 配置
└── .env.example          # 环境变量模板
```

---

### 第三步：配置环境变量

**在宝塔终端执行**：
```bash
cd /www/wwwroot/guoxue.073955.com
cp .env.example .env
nano .env
```

**填写以下内容**：

```bash
# 服务端基础配置
NODE_ENV=production
PORT=3110
HOST=0.0.0.0
PUBLIC_BASE_URL=https://guoxue.073955.com

# 管理员 API 密钥（用于生成授权码）
# 请替换为强随机字符串，至少 32 位
ADMIN_API_KEY=你的随机密钥

# 数据和工作流目录（不需要改）
DATA_DIR=./data
WORKFLOW_DIR=./workflows
```

**生成随机密钥**：
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```
将输出的字符串填入 `ADMIN_API_KEY`。

保存退出：`Ctrl+O` → `Enter` → `Ctrl+X`

---

### 第四步：安装依赖

```bash
npm install --production
```

**预期输出**：
```
added 15 packages in 8s
```

---

### 第五步：创建必要目录

```bash
mkdir -p data logs
```

---

### 第六步：启动服务

**使用 PM2 启动**：
```bash
pm2 start ecosystem.config.js
pm2 save
pm2 startup
```

**查看日志**：
```bash
pm2 logs guoxue-server
```

**预期输出**：
```
✅ 服务已启动: http://0.0.0.0:3110
📍 公共地址: https://guoxue.073955.com
🔐 管理接口: https://guoxue.073955.comadmin/codes
```

---

### 第七步：配置反向代理

**在宝塔面板**：
1. 进入网站设置
2. 点击"反向代理"
3. 添加反向代理：
   - 代理名称：`guoxue-mcp`
   - 目标URL：`http://127.0.0.1:3110`
   - 发送域名：`$host`
4. 保存

---

### 第八步：验证部署

**测试健康检查**：
```bash
curl https://guoxue.073955.com/health
```

**预期返回**：
```json
{"status":"ok","service":"guoxue-chuanyue","version":"1.1.0"}
```

**测试激活页面**：
在浏览器打开：`https://guoxue.073955.com/redeem`
应该看到激活表单。

---

## 🔑 生成授权码

### 方法一：使用 curl 命令

```bash
curl -X POST https://guoxue.073955.com/admin/codes/generate \
  -H "Authorization: Bearer 你的ADMIN_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"count": 1, "expiresInDays": 365}'
```

**返回示例**：
```json
{
  "codes": [
    {
      "id": "abc123...",
      "code": "GX-A2B3-C4D5-E6F7-G8H9-J0K1",
      "codeDisplay": "GX-A2B****-J0K1",
      "expiresAt": "2027-09-08T...",
      "createdAt": "2026-09-08T..."
    }
  ]
}
```

**参数说明**：
- `count`：生成数量（1-100）
- `expiresInDays`：有效天数（不填则永久）

### 方法二：使用 Postman

1. **请求类型**：POST
2. **URL**：`https://guoxue.073955.com/admin/codes/generate`
3. **Headers**：
   - `Authorization`: `Bearer 你的ADMIN_API_KEY`
   - `Content-Type`: `application/json`
4. **Body**（raw JSON）：
   ```json
   {
     "count": 5,
     "expiresInDays": 365
   }
   ```

---

## 📱 用户激活流程

### 用户操作步骤

1. **访问激活页面**：https://guoxue.073955.com/redeem
2. **填写信息**：
   - 手机号：用于绑定
   - 授权码：管理员提供的 `GX-XXXX-XXXX...`
3. **点击激活**
4. **复制信息**：
   - Client ID
   - Client Secret

### 配置 Claude Desktop

用户需要在 Claude Desktop 的 MCP 配置中添加：

```json
{
  "mcpServers": {
    "guoxue-chuanyue": {
      "type": "http",
      "url": "https://guoxue.073955.com/mcp",
      "auth": "oauth",
      "oauth": {
        "clientId": "用户的Client ID",
        "clientSecret": "用户的Client Secret"
      }
    }
  }
}
```

---

## 🛠️ 管理和维护

### 查看所有授权码

```bash
curl -H "Authorization: Bearer 你的ADMIN_API_KEY" \
  https://guoxue.073955.com/admin/codes
```

### 撤销授权码

```bash
curl -X POST \
  -H "Authorization: Bearer 你的ADMIN_API_KEY" \
  https://guoxue.073955.com/admin/codes/授权码ID/revoke
```

### 查看服务状态

```bash
pm2 status
pm2 logs guoxue-server
pm2 monit
```

### 重启服务

```bash
pm2 restart guoxue-server
```

### 停止服务

```bash
pm2 stop guoxue-server
```

### 查看数据库

```bash
cd /www/wwwroot/guoxue.073955.com/data
sqlite3 licenses.sqlite
```

SQLite 命令：
```sql
.tables                          -- 查看所有表
SELECT * FROM customers;         -- 查看用户
SELECT * FROM activation_codes;  -- 查看授权码
SELECT * FROM devices;           -- 查看设备
.quit                           -- 退出
```

---

## 🔒 安全建议

1. **定期备份数据库**：
   ```bash
   cp data/licenses.sqlite data/licenses-backup-$(date +%Y%m%d).sqlite
   ```

2. **保护 ADMIN_API_KEY**：
   - 不要提交到 Git
   - 定期更换
   - 使用强随机字符串

3. **监控日志**：
   ```bash
   tail -f logs/out.log logs/err.log
   ```

4. **限制管理接口访问**：
   在宝塔防火墙中限制 `/admin/*` 只允许特定 IP 访问

---

## ❓ 常见问题

### Q: 用户激活后显示 "无效的访问令牌"

**原因**：服务重启后内存中的 OAuth clients 丢失

**解决**：用户需要重新访问 `/redeem` 页面激活（使用相同的手机号和授权码）

### Q: workflow 文件乱码

**原因**：文件编码问题

**解决**：确保 workflow 文件是 UTF-8 编码

### Q: Node.js 版本不够

**需要 22+**：因为使用了内置的 `node:sqlite`

**升级方法**：
```bash
# 使用 nvm
nvm install 22
nvm use 22
```

### Q: 端口 3110 被占用

**修改端口**：
1. 编辑 `.env`：`PORT=其他端口`
2. 重启服务：`pm2 restart guoxue-server`

---

## 📊 监控和日志

### 实时日志

```bash
pm2 logs guoxue-server --lines 100
```

### 错误日志

```bash
tail -f /www/wwwroot/guoxue.073955.com/logs/err.log
```

### 审计日志（数据库）

```bash
cd /www/wwwroot/guoxue.073955.com/data
sqlite3 licenses.sqlite "SELECT * FROM audit_log ORDER BY created_at DESC LIMIT 20;"
```

---

## 🎯 下一步

部署完成后：
1. ✅ 生成测试授权码
2. ✅ 自己测试激活流程
3. ✅ 验证 Claude Desktop 能正常使用
4. ✅ 分发授权码给用户

有问题请参考开发文档或联系技术支持。
