# 准备公开发布版本 - 操作指南

## 目标
创建一个只包含公开文件的干净版本，用于发布到 GitHub 或插件市场。

## 操作步骤

### 第 1 步：创建公开版本目录

```bash
# 在当前目录的上级创建公开版本目录
cd ..
mkdir zengmei-yuer-guofeng-public-plugin
cd zengmei-yuer-guofeng-public-plugin
```

### 第 2 步：复制公开文件

```bash
# 复制插件结构
cp -r ../guoxue-skill/.codex-plugin .
cp -r ../guoxue-skill/skills .

# 复制配置文件
cp ../guoxue-skill/.mcp.json .
cp ../guoxue-skill/mcp-proxy.js .
cp ../guoxue-skill/package.json .
cp ../guoxue-skill/.gitignore .
cp ../guoxue-skill/README.md .

# Windows 用户使用 PowerShell：
# Copy-Item -Recurse ..\guoxue-skill\.codex-plugin .
# Copy-Item -Recurse ..\guoxue-skill\skills .
# Copy-Item ..\guoxue-skill\.mcp.json .
# Copy-Item ..\guoxue-skill\mcp-proxy.js .
# Copy-Item ..\guoxue-skill\package.json .
# Copy-Item ..\guoxue-skill\.gitignore .
# Copy-Item ..\guoxue-skill\README.md .
```

### 第 3 步：验证文件清单

```bash
# 检查目录结构
ls -R

# 应该看到：
# .codex-plugin/plugin.json
# skills/guoxue-chuanyue/SKILL.md
# .mcp.json
# mcp-proxy.js
# package.json
# .gitignore
# README.md

# 确保没有私有文件
ls | grep -E "(01B_|02_|03_|AGENTS|架构设计)"
# 应该没有任何输出
```

### 第 4 步：创建 LICENSE 文件

创建 `LICENSE` 文件（商业许可证示例）：

```bash
cat > LICENSE << 'EOF'
商业软件许可证

版权所有 (c) 2024 曾美团队

本软件及相关文档（以下简称"软件"）的版权归曾美团队所有。

授权范围：
1. 本软件仅授权给已购买授权码的最终用户使用
2. 每个授权码仅可在一台设备上使用
3. 未经许可不得复制、修改、分发、出售本软件
4. 未经许可不得对本软件进行逆向工程、反编译或反汇编

免责声明：
本软件按"现状"提供，不提供任何明示或暗示的保证，包括但不限于适销性、
特定用途适用性和非侵权性的保证。在任何情况下，作者或版权持有人均不对
任何索赔、损害或其他责任负责。

联系方式：
官网：https://guoxue.073955.com
管理后台：https://guoxue.073955.com/admin
EOF
```

### 第 5 步：初始化 Git 仓库

```bash
# 初始化 Git
git init

# 添加所有文件
git add .

# 查看将要提交的文件
git status

# 确保只有公开文件
git commit -m "Initial release: 曾美团队·育儿国风穿越插件 v1.0.0

- 远程授权插件结构
- 一机一码绑定机制
- MCP 代理实现
- 完整使用文档"
```

### 第 6 步：安全检查

运行以下命令确保没有敏感内容：

```bash
# 检查是否包含敏感关键词
grep -r "规则本体" . 2>/dev/null
grep -r "范例库" . 2>/dev/null
grep -r "AGENTS" . 2>/dev/null
grep -r "01B_" . 2>/dev/null
grep -r "02_" . 2>/dev/null
grep -r "password" . 2>/dev/null
grep -r "secret" . 2>/dev/null

# 如果有结果，仔细检查是否是文档中的说明性文字
# 真正的敏感文件不应该存在
```

### 第 7 步：推送到 GitHub

```bash
# 在 GitHub 创建新仓库（名称：zengmei-yuer-guofeng-plugin）
# 然后执行：

git remote add origin https://github.com/你的用户名/zengmei-yuer-guofeng-plugin.git
git branch -M main
git push -u origin main
```

### 第 8 步：创建发布标签

```bash
# 创建版本标签
git tag -a v1.0.0 -m "Release v1.0.0

功能：
- 远程授权插件
- 一机一码绑定
- 古今对话短剧生成
- 八阶段工作流"

# 推送标签
git push origin v1.0.0
```

### 第 9 步：完善 GitHub 仓库

在 GitHub 网页上：

1. **Settings → Options**
   - Description: `曾美团队·育儿国风穿越 - 古今对话短剧AI视频生产插件`
   - Website: `https://guoxue.073955.com`
   - Topics: `claude-plugin`, `mcp-server`, `ai-video`, `chinese`

2. **创建 Release**
   - 点击 "Releases" → "Create a new release"
   - 选择标签 v1.0.0
   - 标题: `v1.0.0 - 首次发布`
   - 描述发布内容
   - 可以附加 .zip 包

3. **编辑 README**（如需要）
   - 添加徽章（Build Status, Version 等）
   - 添加截图或演示视频

## 📋 验证清单

发布前确认：

- [ ] 目录中只有公开文件
- [ ] 没有 `01B_规则本体_*.md`
- [ ] 没有 `02_赛道范例库_*.md`
- [ ] 没有 `03_使用说明_*.md`
- [ ] 没有 `AGENTS.md`
- [ ] 没有 `架构设计_*.md`
- [ ] 没有 `.env` 或密钥文件
- [ ] LICENSE 文件已创建
- [ ] README.md 不包含内部信息
- [ ] .gitignore 正确配置
- [ ] Git 历史中没有敏感信息

## 🔄 后续更新流程

当需要更新插件时：

1. 在私有目录（guoxue-skill）开发和测试
2. 更新版本号
3. 复制变更文件到公开目录
4. 提交并打新标签
5. 推送到 GitHub

```bash
# 更新流程示例
cd ../guoxue-skill
# ... 修改代码 ...

cd ../zengmei-yuer-guofeng-public-plugin
cp ../guoxue-skill/mcp-proxy.js .
cp ../guoxue-skill/.codex-plugin/plugin.json .codex-plugin/

# 更新版本号为 1.0.1
git add .
git commit -m "Update to v1.0.1: 修复授权刷新问题"
git tag -a v1.0.1 -m "v1.0.1"
git push origin main
git push origin v1.0.1
```

## ⚠️ 重要提醒

1. **绝对不要**在公开仓库提交私有文件
2. **定期检查** Git 历史确保没有敏感信息泄露
3. **如果不慎泄露**，立即删除仓库并更换所有密钥
4. **保持两个目录分离**：
   - `guoxue-skill/` - 开发版（包含私有文件）
   - `zengmei-yuer-guofeng-public-plugin/` - 发布版（仅公开文件）

---

完成以上步骤后，你的插件就可以在 GitHub 上公开展示了！
