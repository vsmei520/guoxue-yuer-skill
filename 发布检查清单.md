# 插件发布清单

## 公开发布前必须检查的项目

### ✅ 必须包含的文件（公开版本）

```
zengmei-yuer-guofeng-plugin/
├── .codex-plugin/
│   └── plugin.json          ✅ 插件元数据
├── skills/
│   └── guoxue-chuanyue/
│       └── SKILL.md         ✅ 薄包装入口（不含核心规则）
├── .mcp.json                ✅ MCP 服务器配置
├── mcp-proxy.js             ✅ 本地代理（授权+转发）
├── package.json             ✅ Node.js 包信息
├── .gitignore               ✅ Git 忽略规则
├── README.md                ✅ 使用说明
└── LICENSE                  ⚠️ 需要添加（商业许可证）
```

### ❌ 严禁包含的文件（必须保持私有）

- ❌ `01B_规则本体_10s版v3.9.md` - 核心规则本体
- ❌ `02_赛道范例库_v1.7.md` - 范例库
- ❌ `03_使用说明_人类阅读.md` - 详细使用说明（包含内部信息）
- ❌ `AGENTS.md` - Agent 配置
- ❌ `SKILL.md` (根目录) - 完整 Skill 定义
- ❌ `架构设计_一机一码授权系统.md` - 架构设计文档
- ❌ `.env` 或任何包含密钥的文件
- ❌ 服务端源码
- ❌ 数据库 schema
- ❌ 内部部署脚本

### 🔍 发布前安全检查清单

1. **文件内容检查**
   - [ ] `mcp-proxy.js` 中没有硬编码的密钥或敏感信息
   - [ ] `REMOTE_MCP_URL` 指向正确的生产服务器
   - [ ] `skills/guoxue-chuanyue/SKILL.md` 不包含核心规则原文
   - [ ] README.md 不包含内部架构细节

2. **功能验证**
   - [ ] `activate_license` 工具正常打开授权页面
   - [ ] `auth_status` 工具正确显示授权状态
   - [ ] 设备指纹生成和保存正常
   - [ ] 授权信息加密存储正常（Windows DPAPI / macOS base64）
   - [ ] 与远程 MCP 服务器通信正常

3. **服务端检查**
   - [ ] 远程 MCP 端点 `https://guoxue.073955.com/mcp` 已部署
   - [ ] 激活端点 `https://guoxue.073955.com/activate` 已部署
   - [ ] 服务端正确验证设备ID和授权码
   - [ ] 服务端返回的 tools/list 包含所需工具
   - [ ] 服务端不在响应中返回核心 Skill 原文

4. **授权流程测试**
   - [ ] 首次激活流程完整可用
   - [ ] 一机一码绑定正常工作
   - [ ] 同一授权码在第二台设备被正确拒绝
   - [ ] Token 刷新机制正常（如已实现）
   - [ ] 设备解绑后可重新激活
   - [ ] 授权撤销后立即失效

5. **文档检查**
   - [ ] README.md 包含完整的安装和使用说明
   - [ ] 推荐启动词准确可用
   - [ ] 授权获取方式清晰
   - [ ] 常见问题覆盖主要疑问
   - [ ] 隐私政策说明清楚

6. **法律和合规**
   - [ ] LICENSE 文件明确商业软件性质
   - [ ] 版权声明正确
   - [ ] 隐私政策完整
   - [ ] 服务条款清晰

### 📦 发布步骤

#### 1. 准备公开仓库

```bash
# 创建新目录用于公开版本
mkdir -p ../zengmei-yuer-guofeng-public-plugin
cd ../zengmei-yuer-guofeng-public-plugin

# 只复制公开文件
cp -r ../guoxue-skill/.codex-plugin .
cp -r ../guoxue-skill/skills .
cp ../guoxue-skill/.mcp.json .
cp ../guoxue-skill/mcp-proxy.js .
cp ../guoxue-skill/package.json .
cp ../guoxue-skill/.gitignore .
cp ../guoxue-skill/README.md .

# 创建 LICENSE 文件
# （需要手动创建商业许可证文本）
```

#### 2. 验证文件清单

```bash
# 确保没有私有文件
ls -la
# 应该只看到上述列出的公开文件
```

#### 3. 初始化 Git 仓库

```bash
git init
git add .
git commit -m "Initial release: 曾美团队·育儿国风穿越插件 v1.0.0"
```

#### 4. 推送到 GitHub

```bash
# 创建远程仓库后
git remote add origin https://github.com/你的用户名/zengmei-yuer-guofeng-plugin.git
git branch -M main
git push -u origin main
```

#### 5. 创建发布标签

```bash
git tag -a v1.0.0 -m "Release version 1.0.0"
git push origin v1.0.0
```

### 🚨 紧急情况处理

如果不小心将私有文件推送到公开仓库：

1. **立即删除仓库**（GitHub Settings → Delete this repository）
2. **更换所有密钥和令牌**
3. **清理 Git 历史**后重新创建仓库
4. **审查泄露的内容**，评估安全影响
5. **通知相关人员**

### 📝 版本更新流程

更新插件时：

1. 在私有仓库开发和测试
2. 更新版本号（package.json 和 plugin.json）
3. 更新 README.md 的更新日志
4. 重新执行上述发布检查清单
5. 复制到公开目录
6. 提交并打标签
7. 在服务端更新插件版本信息

### ✅ 最终检查命令

在公开仓库目录运行：

```bash
# 检查是否包含敏感关键词
grep -r "规则本体" .
grep -r "范例库" .
grep -r "AGENTS" .
grep -r "password" .
grep -r "secret" .
grep -r "private" .

# 应该没有匹配结果（或只有注释中的说明）
```

### 📞 联系方式

- **管理后台**: https://guoxue.073955.com/admin
- **技术支持**: 曾美团队
- **官网**: https://guoxue.073955.com

---

**重要提醒**: 核心 Skill 规则是核心竞争力，务必确保永远不会出现在公开代码库中。
