# 服务端实现指南

## 一、服务端技术栈推荐

- **后端框架**: Node.js (Express/Fastify) 或 Python (FastAPI)
- **数据库**: PostgreSQL
- **缓存**: Redis
- **部署**: Docker + Nginx

## 二、必须实现的 API 端点

### 1. 激活端点

```http
POST https://guoxue.073955.com/activate
Content-Type: application/json

{
  "phone": "13800138000",
  "code": "用户输入的授权码",
  "deviceId": "gx-abc123..."
}
```

**返回**:
```json
{
  "accessToken": "atk_xxx",
  "refreshToken": "rtk_xxx",
  "expiresIn": 3600
}
```

**服务端逻辑**:
1. 验证授权码是否有效
2. 检查授权码是否已绑定设备
3. 如果未绑定，绑定到当前 deviceId
4. 如果已绑定其他设备，返回错误
5. 生成并返回 token

### 2. MCP 端点

```http
POST https://guoxue.073955.com/mcp
Authorization: Bearer atk_xxx
X-Device-ID: gx-abc123...
Content-Type: application/json

{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "tools/list",
  "params": {}
}
```

**返回**:
```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "tools": [
      {
        "name": "generate_stage_1",
        "description": "生成第一阶段：钩子分析+典故取材",
        "inputSchema": {
          "type": "object",
          "properties": {
            "topic": {"type": "string"},
            "structure": {"type": "string"}
          }
        }
      }
      // ... 其他阶段的工具
    ]
  }
}
```

**服务端逻辑**:
1. 验证 token 和设备绑定
2. 根据 method 分发请求
3. `tools/list` 返回可用工具列表
4. `tools/call` 执行具体工具，调用 AI 模型生成内容

### 3. 核心工具执行逻辑

```http
POST https://guoxue.073955.com/mcp
Authorization: Bearer atk_xxx
Content-Type: application/json

{
  "jsonrpc": "2.0",
  "id": 2,
  "method": "tools/call",
  "params": {
    "name": "generate_stage_1",
    "arguments": {
      "topic": "孩子不爱吃饭",
      "structure": "现代妈妈焦虑 → 穿越见孟母 → 获得启发回到现代"
    }
  }
}
```

**服务端内部流程**:
```javascript
1. 验证授权和设备
2. 读取私有 Skill 文件（01B_规则本体_10s版v3.9.md）
3. 构建 AI prompt（包含规则 + 用户输入）
4. 调用 OpenAI/Claude API
5. 解析 AI 输出
6. 返回结果给用户
```

**返回**:
```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "【第一阶段：钩子分析+典故取材】\n\n选题痛点分析：\n..."
      }
    ]
  }
}
```

## 三、数据库设计

### 核心表结构

```sql
-- 用户表
CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  phone VARCHAR(20) UNIQUE NOT NULL,
  created_at TIMESTAMP DEFAULT NOW()
);

-- 授权码表
CREATE TABLE redemption_codes (
  id SERIAL PRIMARY KEY,
  code_hash VARCHAR(64) UNIQUE NOT NULL,  -- 存储哈希，不存明文
  user_id INTEGER REFERENCES users(id),
  status VARCHAR(20) DEFAULT 'active',    -- active, used, revoked
  max_devices INTEGER DEFAULT 1,
  expires_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW()
);

-- 设备表
CREATE TABLE devices (
  id SERIAL PRIMARY KEY,
  device_id_hash VARCHAR(64) UNIQUE NOT NULL,
  user_id INTEGER REFERENCES users(id),
  code_id INTEGER REFERENCES redemption_codes(id),
  status VARCHAR(20) DEFAULT 'active',     -- active, unbound, revoked
  bound_at TIMESTAMP DEFAULT NOW(),
  last_used_at TIMESTAMP
);

-- Token 表
CREATE TABLE access_tokens (
  id SERIAL PRIMARY KEY,
  token_hash VARCHAR(64) UNIQUE NOT NULL,
  user_id INTEGER REFERENCES users(id),
  device_id INTEGER REFERENCES devices(id),
  expires_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW()
);

-- 使用日志表（可选）
CREATE TABLE usage_logs (
  id SERIAL PRIMARY KEY,
  user_id INTEGER REFERENCES users(id),
  device_id INTEGER REFERENCES devices(id),
  tool_name VARCHAR(100),
  input_data JSONB,
  created_at TIMESTAMP DEFAULT NOW()
);
```

## 四、快速开发方案

### 选项 1: Node.js + Express（推荐新手）

```bash
# 创建服务端项目
mkdir guoxue-server
cd guoxue-server
npm init -y
npm install express pg redis jsonwebtoken crypto bcrypt dotenv cors
```

创建 `server.js`:
```javascript
const express = require('express');
const crypto = require('crypto');
const app = express();

app.use(express.json());

// 激活端点
app.post('/activate', async (req, res) => {
  const { phone, code, deviceId } = req.body;
  
  // TODO: 验证授权码
  // TODO: 检查设备绑定
  // TODO: 生成 token
  
  res.json({
    accessToken: 'atk_' + crypto.randomBytes(32).toString('hex'),
    refreshToken: 'rtk_' + crypto.randomBytes(32).toString('hex'),
    expiresIn: 3600
  });
});

// MCP 端点
app.post('/mcp', async (req, res) => {
  const { method, params } = req.body;
  
  if (method === 'tools/list') {
    return res.json({
      jsonrpc: '2.0',
      id: req.body.id,
      result: {
        tools: [
          {
            name: 'generate_stage_1',
            description: '生成第一阶段：钩子分析+典故取材',
            inputSchema: {
              type: 'object',
              properties: {
                topic: { type: 'string' },
                structure: { type: 'string' }
              }
            }
          }
        ]
      }
    });
  }
  
  if (method === 'tools/call') {
    // TODO: 调用 AI 模型
    // TODO: 读取 Skill 文件
    // TODO: 生成内容
  }
  
  res.json({ error: 'Not implemented' });
});

app.listen(3000, () => {
  console.log('Server running on port 3000');
});
```

### 选项 2: Python + FastAPI（推荐熟悉 Python 的）

```bash
pip install fastapi uvicorn sqlalchemy psycopg2-binary redis pyjwt openai
```

### 选项 3: 使用云服务快速部署

- **Vercel/Netlify**: 无服务器函数
- **Railway/Render**: 一键部署
- **AWS Lambda**: Serverless
- **Cloudflare Workers**: 边缘计算

## 五、开发优先级

### 阶段 1: MVP（最小可用版本）
1. ✅ 实现激活端点（硬编码授权码测试）
2. ✅ 实现 tools/list 端点
3. ✅ 实现一个工具（generate_stage_1）
4. ✅ 连接 OpenAI API
5. ✅ 读取本地 Skill 文件

### 阶段 2: 授权系统
1. ✅ 实现数据库
2. ✅ 授权码生成和管理
3. ✅ 一机一码绑定逻辑
4. ✅ Token 验证和刷新

### 阶段 3: 完整功能
1. ✅ 实现所有 8 个阶段工具
2. ✅ 会话管理
3. ✅ 阶段确认机制
4. ✅ 使用统计

### 阶段 4: 管理后台
1. ✅ 管理员登录
2. ✅ 授权码管理
3. ✅ 用户管理
4. ✅ 设备解绑

## 六、本地测试

1. 启动服务端: `node server.js`
2. 修改插件配置，指向本地:
   ```javascript
   // mcp-proxy.js
   const REMOTE_MCP_URL = "http://localhost:3000/mcp";
   ```
3. 测试激活流程
4. 测试工具调用

## 七、部署上线

1. 购买域名: `guoxue.073955.com`
2. 部署服务器（推荐使用 Docker）
3. 配置 HTTPS（Let's Encrypt）
4. 配置 Nginx 反向代理
5. 设置防火墙和安全规则
6. 监控和日志

## 八、安全建议

- ✅ 所有 API 使用 HTTPS
- ✅ Token 只存储哈希值
- ✅ 实现请求频率限制
- ✅ 记录审计日志
- ✅ 定期备份数据库
- ✅ 实现异常监控

---

需要详细的服务端代码示例吗？我可以帮你生成完整的 Express 或 FastAPI 项目模板。
