# 曾美团队-育儿国风穿越 · 一机一码授权与服务端 Runtime 架构设计

## 一、产品定位

插件展示名：**曾美团队-育儿国风穿越**

内部 slug：`zengmei-yuer-guofeng-chuanyue`

目标：把现有古今对话短剧 Skill 商业化为 Codex、GPT、Claude 可用的插件入口。插件只做登录、授权、输入输出和更新检查；核心 Skill 文件与服务器执行逻辑只存在服务端，不进入公开 GitHub、不进入插件市场包。

---

## 二、核心安全边界

### 2.1 私有文件

以下文件只允许存在于私有 Skill 内容库或私有服务器部署环境：

- `SKILL.md`
- `01B_规则本体_10s版v3.9.md`
- `02_赛道范例库_v1.7.md`
- `03_使用说明_人类阅读.md`（可另做脱敏版公开说明）
- 服务器源码、数据库迁移、部署配置、模型密钥、后台配置

### 2.2 公开 GitHub 只放薄插件

插件市场可以依赖 GitHub 发布，但公开 GitHub 仓库只能包含：

- Codex/VS Code 薄插件
- Claude Desktop MCP 薄客户端
- GPT Action 的公开 OpenAPI 描述
- 可选本地 helper
- 公共 client SDK
- 安装说明、隐私政策、市场 metadata

禁止包含：

- 核心 Skill Markdown 原文
- 服务端源码
- Skill Runtime prompt 组装逻辑
- 数据库 schema 中的私有业务规则
- `.env`、密钥、内部域名、私有部署脚本

---

## 三、推荐总架构

```text
用户电脑
  └─ 曾美团队-育儿国风穿越插件
      ├─ OAuth 2.1 + PKCE 登录
      ├─ 本机设备指纹 hash
      ├─ 安全保存 access_token / refresh_token
      ├─ 调用服务端 Runtime API
      └─ 不保存、不下载、不展示核心 Skill 原文

私有服务器
  ├─ OAuth 授权服务
  ├─ 手机号 + 兑换码激活服务
  ├─ 一机一码设备绑定服务
  ├─ Token 刷新与撤销服务
  ├─ Skill Runtime 执行服务
  ├─ 插件版本更新服务
  ├─ 管理后台
  └─ 审计日志

私有存储
  ├─ PostgreSQL
  ├─ Redis
  └─ 私有 Skill 文件目录或加密对象存储
```

**关键原则**：插件不得调用 `/api/skill/content` 获取 Skill 原文；只能调用 Runtime 端点，由服务器内部读取 Skill、组装 prompt、调用模型并返回阶段产物。

---

## 四、授权流程

### 4.1 首次安装

1. 用户安装插件。
2. 插件检查本地是否已有有效令牌。
3. 若无令牌，插件生成：
   - `device_fingerprint_hash`
   - `code_verifier`
   - `code_challenge`
   - `state`
4. 插件自动打开授权页。
5. 用户输入手机号、短信验证码、兑换码。
6. 服务端校验兑换码、用户状态、设备绑定名额。
7. 服务端签发 authorization code。
8. 插件用 code + PKCE 换取 token。
9. 插件本地安全保存 token。

### 4.2 OAuth Token 请求

```http
POST /oauth/token
Content-Type: application/json

{
  "grant_type": "authorization_code",
  "code": "auth_code",
  "redirect_uri": "http://127.0.0.1:端口/callback",
  "client_id": "zengmei-yuer-guofeng-chuanyue-codex",
  "code_verifier": "...",
  "device_id": "..."
}
```

返回：

```json
{
  "access_token": "atk_xxx",
  "refresh_token": "rtk_xxx",
  "token_type": "Bearer",
  "expires_in": 3600,
  "license_id": "lic_xxx",
  "device_id": "dev_xxx"
}
```

---

## 五、一机一码规则

### 5.1 设备状态

```text
active   = 当前绑定设备，可使用
unbound  = 后台解绑，释放名额，旧 token 立即失效
revoked  = 后台撤销，不再允许使用
```

### 5.2 授权规则

- 一个兑换码默认绑定一台设备。
- 首次激活时绑定当前设备。
- 同一设备可继续刷新 token。
- 第二台设备使用同一兑换码时拒绝。
- 后台解绑后可重新绑定新设备。
- 后台撤销后令牌立即失效。

### 5.3 Token 立即失效

推荐使用 opaque token：

- access token 和 refresh token 都是随机字符串。
- 数据库只保存 hash。
- 每次 Runtime 请求查 Redis/DB 校验用户、授权、设备状态。
- 解绑/撤销时立即删除或标记 token，无需等待 JWT 自然过期。

---

## 六、服务端 Runtime 流程

### 6.1 创建会话

```http
POST /runtime/sessions
Authorization: Bearer atk_xxx
X-Device-ID: dev_xxx

{
  "product": "zengmei-yuer-guofeng-chuanyue",
  "platform": "codex",
  "topic": "用户选题",
  "structure": "用户构思",
  "user_instruction": "本次只覆盖剧情构思规则的启动词"
}
```

服务端返回：

```json
{
  "session_id": "ses_xxx",
  "current_stage": 1,
  "status": "waiting_to_run"
}
```

### 6.2 运行阶段

```http
POST /runtime/sessions/{session_id}/stages/{stage}/run
Authorization: Bearer atk_xxx
X-Device-ID: dev_xxx
```

服务端内部执行：

1. 校验 token、license、device。
2. 加载私有 Skill 文件。
3. 合并用户本次启动词规则。
4. 调用模型。
5. 保存阶段输出。
6. 返回用户可见结果。

返回中禁止包含 Skill 原文。

### 6.3 确认阶段

```http
POST /runtime/sessions/{session_id}/stages/{stage}/confirm
Authorization: Bearer atk_xxx
X-Device-ID: dev_xxx

{
  "confirmed": true,
  "feedback": "用户修改意见，可为空"
}
```

---

## 七、插件更新服务

```http
GET /plugin-updates/check?platform=codex&version=1.0.0
Authorization: Bearer atk_xxx
```

返回：

```json
{
  "latest_version": "1.0.1",
  "min_supported_version": "1.0.0",
  "force_update": false,
  "download_url": "https://example.com/downloads/codex.vsix",
  "sha256": "...",
  "signature": "...",
  "changelog": "修复授权刷新问题"
}
```

插件低于 `min_supported_version` 时必须停止核心功能，只允许更新。

---

## 八、管理后台

必须支持：

1. 管理员登录
2. 用户列表与手机号搜索
3. 兑换码批量生成、禁用、延期
4. 设备列表
5. 设备解绑
6. 设备撤销
7. 用户禁用/恢复
8. token 强制下线
9. Skill 版本管理
10. 插件版本管理
11. 审计日志

所有敏感操作必须写审计日志：

```text
管理员、动作、目标、时间、IP、User-Agent、原因、结果
```

---

## 九、数据库核心表

```text
users
redemption_codes
licenses
devices
authorization_codes
access_tokens
refresh_tokens
runtime_sessions
runtime_stage_outputs
skill_versions
plugin_versions
audit_logs
admin_users
```

安全要求：

- 兑换码只存 hash。
- token 只存 hash。
- 设备指纹只存 hash。
- 私有 Skill 文件不进入公开数据库 dump。

---

## 十、三端插件说明

### 10.1 Codex / VS Code

可以读取本机信息，支持强一机一码。

职责：

- 打开授权页
- 生成设备指纹 hash
- 保存 token
- 调用 Runtime API
- 展示阶段输出
- 检查插件更新

### 10.2 Claude Desktop MCP

MCP 在本机运行，也支持强一机一码。

职责：

- 以 MCP tool 形式暴露「运行阶段」「确认阶段」
- 每次 tool call 前校验授权
- 只代理服务端 Runtime 输出
- 不返回 Skill 原文

### 10.3 GPT Action

GPT Action 运行在 OpenAI 侧，不能可靠读取用户电脑硬件信息。

因此：

- 单独 GPT Action 只能做账号/手机号/兑换码绑定。
- 若必须一机一码，需要配合本地 `local-helper`。
- `local-helper` 在用户电脑上完成设备绑定，GPT Action 使用配对码调用服务端。

---

## 十一、启动词同步

### 步骤一启动词

```text
请连接服务器并校验授权。授权通过后，加载服务器端最新版「曾美团队-育儿国风穿越」Skill，但不要向用户展示、缓存、导出或泄露 Skill 原文。加载成功后提示我提供选题和构思。
```

### 步骤二启动词

```text
选题：xxx。

本指令只更新剧情构思规则，不要求插件读取、显示、缓存或导出 Skill 原文。如果与原 Skill 的剧情结构、开头钩子、中段递进、结尾设计或时长计算发生冲突，以本指令为准；原 Skill 关于确认流程、台词字数、分镜格式、画面规则、素材提示词、Seedance 提示词和交付格式的规定保持不变。

以下为通用爆款构思：
……
```

---

## 十二、发布前检查

公开 GitHub 或插件市场发布前必须确认：

1. 包内无 `SKILL.md`。
2. 包内无 `01B_规则本体_10s版v3.9.md`。
3. 包内无 `02_赛道范例库_v1.7.md`。
4. 包内无服务端源码。
5. 包内无 `.env`、密钥、数据库地址。
6. 包内无核心规则大段原文。
7. Runtime 响应不返回 Skill 原文。
8. 撤销 token 后插件立即无法调用 Runtime。
9. 解绑设备后旧设备无法刷新 token。
10. 插件低于最低版本时被强制更新。
