# 曾美团队·育儿国风穿越 - 开发文档

## 📐 系统架构

### 整体架构

```
┌─────────────────┐         ┌──────────────────┐
│  Claude Desktop │◄───────►│  服务端 (Node.js) │
│  (用户本地)      │  OAuth  │  guoxue.073955.com│
│                 │         │                  │
│ - 用户 API 密钥 │         │ - OAuth 认证      │
│ - 执行 workflow │         │ - 返回 workflow   │
│                 │         │ - SQLite 存储     │
└─────────────────┘         └──────────────────┘
```

### 关键设计

1. **服务端不调用 AI**
   - 只负责授权验证
   - 返回 workflow 文件内容
   - 不需要配置 AI API 密钥

2. **客户端本地执行**
   - 使用用户在 Claude Desktop 配置的 API
   - 收到 workflow 后本地解析执行
   - 用户承担 AI 费用

3. **OAuth 认证流程**
   - 用户激活 → 获取 Client ID/Secret
   - Claude Desktop 使用 OAuth 认证
   - 通过后可访问 MCP 工具

---

## 🗂️ 项目结构

```
guoxue-skill/
├── server/                    # 服务端
│   ├── src/
│   │   ├── server.js         # 主服务器（Express + MCP SDK）
│   │   └── license-service.js # 授权管理（SQLite）
│   ├── workflows/
│   │   ├── guoxue-chuanyue-main.md     # 核心规则（不公开）
│   │   └── guoxue-chuanyue-examples.md # 范例库（不公开）
│   ├── data/                 # SQLite 数据库目录
│   ├── package.json
│   ├── ecosystem.config.js   # PM2 配置
│   └── .env.example         # 环境变量模板
├── client/market-plugin/     # 市场插件
│   ├── .codex-plugin/
│   │   ├── plugin.json      # 插件元数据
│   │   └── manifest.json    # 清单
│   ├── .mcp.json            # MCP 服务器配置
│   ├── skills/              # 公开说明文件
│   ├── README.md
│   ├── LICENSE
│   └── PRIVACY.md
└── release/                  # 发布包
    ├── guoxue-server-oauth.zip   # 服务端部署包
    └── guoxue-client-oauth.zip   # 客户端插件包
```

---

## 🔑 核心模块

### 1. license-service.js

**职责**：授权码和设备管理

**数据库表结构**：

```sql
-- 用户表
CREATE TABLE customers (
  id TEXT PRIMARY KEY,
  phone TEXT NOT NULL UNIQUE,
  created_at TEXT NOT NULL
);

-- 授权码表
CREATE TABLE activation_codes (
  id TEXT PRIMARY KEY,
  code_hash TEXT NOT NULL UNIQUE,     -- SHA256 哈希
  code_display TEXT NOT NULL,         -- 脱敏显示
  customer_id TEXT REFERENCES customers(id),
  expires_at TEXT,                    -- 过期时间（可选）
  revoked_at TEXT,                    -- 撤销时间
  created_at TEXT NOT NULL
);

-- 设备表
CREATE TABLE devices (
  id TEXT PRIMARY KEY,
  customer_id TEXT NOT NULL REFERENCES customers(id),
  browser_device_id TEXT NOT NULL,    -- 设备标识
  label TEXT NOT NULL,                -- 设备描述
  activated_at TEXT NOT NULL,
  last_seen_at TEXT NOT NULL,
  revoked_at TEXT,
  UNIQUE(customer_id, browser_device_id)
);

-- 审计日志
CREATE TABLE audit_log (
  id TEXT PRIMARY KEY,
  customer_id TEXT REFERENCES customers(id),
  action TEXT NOT NULL,
  detail TEXT NOT NULL,
  created_at TEXT NOT NULL
);
```

**核心方法**：

```javascript
// 生成授权码
generateCodes(count, expiresInDays)

// 兑换授权码
redeemCode(phone, code, deviceId, deviceLabel)

// 验证设备
verifyDevice(customerId, deviceId)

// 撤销授权码
revokeCode(codeId)

// 撤销设备
revokeDevice(deviceId)
```

---

### 2. server.js

**职责**：HTTP 服务 + OAuth + MCP

**关键端点**：

| 端点 | 方法 | 说明 |
|------|------|------|
| `/health` | GET | 健康检查 |
| `/redeem` | GET/POST | 激活页面 |
| `/admin/codes/generate` | POST | 生成授权码（需要 Admin API Key） |
| `/admin/codes` | GET | 查看所有授权码 |
| `/admin/codes/:id/revoke` | POST | 撤销授权码 |
| `/auth/*` | - | OAuth 认证端点（MCP SDK 提供） |
| `/mcp/*` | - | MCP 协议端点 |

**MCP 工具**：

```javascript
// 获取核心规则
mcpServer.registerTool("get_guoxue_chuanyue_main_workflow", ...)

// 获取范例库
mcpServer.registerTool("get_guoxue_chuanyue_examples_workflow", ...)
```

---

## 🔐 认证流程

### OAuth 流程详解

```
1. 用户访问 /redeem
   ├─ 输入手机号 + 授权码
   └─ 提交表单

2. 服务端验证
   ├─ 检查授权码是否有效
   ├─ 创建或查找用户
   ├─ 绑定设备
   └─ 生成 Client ID/Secret

3. 用户配置 Claude Desktop
   ├─ 复制 Client ID/Secret
   └─ 写入 MCP 配置

4. Claude Desktop 启动
   ├─ 读取 MCP 配置
   ├─ 向 /auth/authorize 发起 OAuth 请求
   ├─ 获取 access_token
   └─ 使用 access_token 调用 /mcp/*

5. 后续请求
   ├─ Claude 每次调用工具时带上 Bearer token
   ├─ 服务端验证 token
   └─ 返回 workflow 文件内容
```

**OAuth Client 管理**：

```javascript
// 内存存储（重启丢失）
const oauthClients = new Map();

function getOrCreateClient(customerId, deviceId) {
  const clientKey = `${customerId}:${deviceId}`;
  if (!oauthClients.has(clientKey)) {
    const clientId = randomBytes(16).toString("hex");
    const clientSecret = randomBytes(32).toString("hex");
    oauthClients.set(clientKey, { clientId, clientSecret, customerId, deviceId });
  }
  return oauthClients.get(clientKey);
}
```

**注意**：服务重启后，用户需要重新访问 `/redeem` 激活（会生成新的 Client ID/Secret）。

---

## 🛠️ 本地开发

### 环境要求

- Node.js >= 22.0.0（需要内置 `node:sqlite`）
- Git

### 克隆项目

```bash
git clone https://github.com/vsmei520/guoxue-yuer-skill.git
cd guoxue-yuer-skill/server
```

### 安装依赖

```bash
npm install
```

### 配置环境变量

```bash
cp .env.example .env
nano .env
```

填写：
```bash
NODE_ENV=development
PORT=3110
HOST=127.0.0.1
PUBLIC_BASE_URL=http://localhost:3110
ADMIN_API_KEY=dev-test-key-12345
DATA_DIR=./data
WORKFLOW_DIR=./workflows
```

### 创建必要目录

```bash
mkdir -p data logs
```

### 启动服务

```bash
node src/server.js
```

### 测试

1. **访问健康检查**：
   ```bash
   curl http://localhost:3110/health
   ```

2. **生成测试授权码**：
   ```bash
   curl -X POST http://localhost:3110/admin/codes/generate \
     -H "Authorization: Bearer dev-test-key-12345" \
     -H "Content-Type: application/json" \
     -d '{"count": 1}'
   ```

3. **激活测试**：
   访问 http://localhost:3110/redeem

---

## 📝 添加新 Workflow

### 步骤

1. **在 `workflows/` 目录创建新文件**：
   ```bash
   touch workflows/new-workflow.md
   ```

2. **编写 workflow 内容**（Markdown 格式）

3. **在 `server.js` 注册工具**：
   ```javascript
   const workflowFiles = {
     // ...现有的
     newWorkflow: join(workflowDir, "new-workflow.md"),
   };

   mcpServer.registerTool("get_new_workflow", {
     description: "获取新工作流",
     inputSchema: { type: "object", properties: {} },
   }, async () => ({
     content: [{
       type: "text",
       text: readFileSync(workflowFiles.newWorkflow, "utf8"),
     }],
   }));
   ```

4. **更新客户端 `.mcp.json`**：
   ```json
   {
     "mcpServers": {
       "guoxue-chuanyue": {
         "enabled_tools": [
           "get_guoxue_chuanyue_main_workflow",
           "get_guoxue_chuanyue_examples_workflow",
           "get_new_workflow"
         ]
       }
     }
   }
   ```

---

## 🧪 测试

### 单元测试（TODO）

```bash
npm test
```

### 手动测试流程

1. **生成授权码**
2. **访问激活页面**
3. **复制 Client ID/Secret**
4. **配置 Claude Desktop**
5. **重启 Claude Desktop**
6. **测试工具调用**

---

## 🚢 发布流程

### 1. 更新版本号

编辑 `package.json` 和 `src/server.js` 中的版本号。

### 2. 打包服务端

```bash
cd server
powershell -Command "Compress-Archive -Path src,workflows,package.json,ecosystem.config.js,.env.example -DestinationPath ../release/guoxue-server-oauth.zip -Force"
```

### 3. 打包客户端

```bash
cd client/market-plugin
powershell -Command "Compress-Archive -Path .codex-plugin,.mcp.json,package.json,skills,README.md,LICENSE,PRIVACY.md -DestinationPath ../../release/guoxue-client-oauth.zip -Force"
```

### 4. 测试发布包

1. 在干净环境解压
2. 按部署指南操作
3. 验证所有功能

### 5. 推送到 GitHub

```bash
git add .
git commit -m "发布 v1.2.0"
git push origin main
```

**注意**：`.gitignore` 已排除核心 workflow 文件，不会泄露。

---

## ⚠️ 安全注意事项

### 1. 核心文件保护

**不能上传到 GitHub**：
- `workflows/*.md` - 核心规则
- `.env` - 环境变量
- `data/*.sqlite` - 数据库

**已在 `.gitignore` 排除**：
```
workflows/*.md
.env
data/
```

### 2. API 密钥管理

- `ADMIN_API_KEY`：用于管理接口，必须强随机
- 定期更换
- 不要硬编码在代码中

### 3. 授权码安全

- 明文授权码只在生成时返回一次
- 数据库只存 SHA256 哈希
- 过期后自动失效

### 4. OAuth Client

- Client Secret 只在激活时显示一次
- 内存存储，重启丢失（用户需重新激活）
- 未来可改为持久化存储

---

## 🐛 调试技巧

### 查看请求日志

在 `server.js` 添加：
```javascript
app.use((req, res, next) => {
  console.log(`${req.method} ${req.path}`, req.headers);
  next();
});
```

### 查看 OAuth 流程

```javascript
function logOAuthEvent(event, detail) {
  console.info(JSON.stringify({ event, ...detail, at: new Date().toISOString() }));
}
```

### 数据库查询

```bash
sqlite3 data/licenses.sqlite
```

```sql
-- 查看最近激活
SELECT * FROM customers ORDER BY created_at DESC LIMIT 10;

-- 查看未使用的授权码
SELECT * FROM activation_codes WHERE customer_id IS NULL AND revoked_at IS NULL;

-- 查看设备活跃时间
SELECT * FROM devices ORDER BY last_seen_at DESC LIMIT 10;
```

---

## 📚 参考资源

- [MCP SDK 文档](https://github.com/modelcontextprotocol/sdk)
- [Express.js 文档](https://expressjs.com/)
- [Node.js SQLite 文档](https://nodejs.org/api/sqlite.html)
- 对标项目：`G:\ali\juqing-skill`

---

## 🤝 贡献指南

暂不接受外部贡献（闭源项目）。

内部开发流程：
1. 创建功能分支
2. 本地测试
3. Code Review
4. 合并到 main
5. 部署到生产

---

## 📞 技术支持

有问题请联系曾美团队技术负责人。
